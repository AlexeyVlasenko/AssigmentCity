import { cities as ActionTypes } from '../actionTypes';

const initialState = {
    list: [],
    search: [],
};

export default (state = initialState, action) => {
    const { type } = action;
    switch (type) {

    }
};
